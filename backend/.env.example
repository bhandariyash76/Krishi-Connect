# Server Configuration
PORT=5000

# MongoDB Configuration
# IMPORTANT: Never commit the actual .env file with real credentials!
# After rotating your credentials in MongoDB Atlas, add them here:
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?appName=<appname>
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@cluster0.kvjy9ez.mongodb.net/krishi_connect?appName=Cluster0

# JWT Configuration (if needed)
# JWT_SECRET=your_jwt_secret_here
